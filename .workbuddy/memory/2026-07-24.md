# 2026-07-24 工作日志

## GlobeLens 每日维护 — Day 13
- 改动类型：边界 bug 修复 / 消除假阴性（避开 Day 12「新增审计维度」）。
- 内容：`analyzer.py` charset 检测兼容两种合法写法——HTML5 `<meta charset>` 与 legacy `<meta http-equiv="Content-Type" content="…; charset=…">`。此前只认前者，老站/非英文站被误报 `charset_missing`（真实假阴性）。回退分支用正则 `charset\s*=\s*([^\s;]+)` 从 http-equiv 的 content 抽值。README「Robust by design」同步。
- 测试：新增 3 例，pytest 33 → 36 passed，零回归。
- Commit：8c185e5 (fix) + 29c9cac (docs)。
- 弯路记录：先误判 `<title>` 用 `.string` 遇嵌套标签会漏读，实测 html.parser 把 title 当 RCDATA、`.string` 本就完整 → 撤销该空改动，改做 charset 修复。
